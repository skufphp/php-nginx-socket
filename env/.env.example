# ========================================
# Конфигурация окружения PHP-Nginx-TCP
# ========================================

# Конфигурация базы данных PostgreSQL
POSTGRES_USER=root
POSTGRES_PASSWORD=root
POSTGRES_DB=
# Хост PostgreSQL для подключений PHP-приложения
PG_HOST=postgres-nginx-socket

# Конфигурация pgAdmin
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
# Примечание: pgAdmin не поддерживает авто-настройку как phpMyAdmin
# Нужно вручную добавить сервер в интерфейсе pgAdmin, используя значение PG_HOST

# Xdebug (необязательно; включается через docker-compose.xdebug.yml по умолчанию при использовании `make xdebug-up`)
# Устанавливайте эти переменные только если хотите включить Xdebug без overlay-файла compose
XDEBUG_MODE=
XDEBUG_START=
XDEBUG_CLIENT_HOST=host.docker.internal